# Base install — kept small so the first run is quick on any machine.
# Everything heavier (spreadsheets, SQL databases, .pptx decks) is an optional
# component installed from the UI's Sources tab; see app/deps.py and
# requirements-optional.txt.
fastapi
uvicorn
anthropic       # the Claude provider; every other provider rides on httpx
httpx
python-multipart
tzdata          # Windows has no system tz database; the SLA clock needs Europe/London
